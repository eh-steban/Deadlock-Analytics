export interface PlayerPath {
  player_slot: number;
  x_min: number;
  y_min: number;
  x_max: number;
  y_max: number;
  x_pos: number[];
  y_pos: number[];
  health: number[];
  move_type: number[];
  combat_type: number[];
}
